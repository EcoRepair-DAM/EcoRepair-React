apiVersion: v1
kind: Service
metadata:
  name: ecorepair-react
spec:
  type: LoadBalancer
  selector:
    app: ecorepair
    tier: react
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
 